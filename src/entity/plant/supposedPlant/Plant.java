package entity.plant.supposedPlant;

import entity.plant.ParentPlant;
import island.Region;

public class Plant extends ParentPlant {
    public Plant() {
    }

    public Plant(Region region) {
        super(region);
    }
}
